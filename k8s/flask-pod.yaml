apiVersion: v1
kind: Pod
metadata:
  name: flask-pod
  labels:
    app: flask-lumberTakeoff
spec:
  containers:
  - name: flask
    image: ericvim/misc-side-projects:latest
    ports:
    - containerPort: 5000
